/* Combos */
#undef COMBO_TERM
#define COMBO_TERM 40  // timeout of 40ms (default is 30ms if omitted)
#define HR_TIMEOUT 30  // timeout on homerow is better a bit shorter

// Left THUMBS
ZMK_COMBO(move_fnc, &mo FNC, LH1 LH0, ALL)

// Right thumbs
ZMK_COMBO(move_sym, &mo SYM, RH0 RH1, ALL)

// Inner thumbs
//ZMK_COMBO(qwerty_34, &tog QWE, LH0 RH0, ALL)

// Right Outer thumb + /
//ZMK_COMBO(move_mov, &mo MOU, RH1 RB4, ALL)

// Left Outer thumb + z
//ZMK_COMBO(move_mov, &mo MOU, LH1 LB4, ALL)

